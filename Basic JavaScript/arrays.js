// // ARRAY = Hum variable me sirf eik value store kerpatay hai, lekin jab hamay eik se ziyada store kerna ho to hum array use kerte hai.

// PUSH POP SHIFT UNSHIFT SPLICE

// let a = [1,2,3,4];

// a.pop() // will remove last value in the array.
// a.push(6) // will add "6" in the starting of the array.
// a.shift() // will remove first value in the array.
// a.unshift(0) // will add "0" in the starting
// a.splice(2,1) // will rmove 3 from the array